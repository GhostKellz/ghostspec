{
  "schema_version": 1,
  "channels": {
    "rc1": {
      "ghostspec_version": "0.9.0-rc1",
      "last_updated": "2025-09-25",
      "zig": {
        "minimum": "0.16.0-dev.164",
        "tested": [
          { "version": "0.16.0-dev.164", "status": "supported", "notes": "Matches build.zig.zon.minimum_zig_version." },
          { "version": "0.16.0-dev.180", "status": "preview", "notes": "No regressions observed in nightly smoke runs." }
        ],
        "blocked": [
          { "version": "0.15.x", "reason": "Missing std.testing APIs used by GhostSpec runner.", "allow_with_force": false },
          { "version": "0.17.0-dev", "reason": "Language changes around test runners; awaiting upstream stabilization.", "allow_with_force": true }
        ]
      },
      "zls": {
        "tested": [
          { "version": "0.13.0", "status": "supported" },
          { "version": "0.14.0-dev", "status": "preview", "notes": "Enable `analysis.lazy = true` to avoid spurious warnings." }
        ],
        "blocked": [
          { "version": "<0.12.0", "reason": "Does not understand Zig 0.16 module layout." }
        ]
      },
      "ci_profiles": {
        "default": {
          "description": "Balance coverage and runtime for PR checks.",
          "fuzz_iterations": 128,
          "fuzz_timeout_ms": 200,
          "benchmark_iterations": 100,
          "allow_force": false
        },
        "hardened": {
          "description": "Nightly soak with higher fuzzing pressure.",
          "fuzz_iterations": 2000,
          "fuzz_timeout_ms": 500,
          "benchmark_iterations": 500,
          "allow_force": true
        }
      }
    }
  },
  "notes": "Update this file whenever new Zig or ZLS releases are verified. Zion surfaces warnings when the consumer environment falls outside the tested ranges."
}
